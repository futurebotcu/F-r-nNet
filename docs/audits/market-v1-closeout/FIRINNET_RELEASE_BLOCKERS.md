# FırınNet — Release Blocker Matrisi

Tarih: 2026-05-15. Kod değişmedi; bu liste yalnızca tespit.

| Priority | Area | Issue | Evidence | Risk | Suggested Fix | Files |
|---|---|---|---|---|---|---|
| P0 | Sosyal | Feed yalnız LocalFeedRepository ile çalışıyor. Supabase implementasyonu yok. Kullanıcılar arası gerçek post/like/comment yok. | `lib/features/feed/providers/feed_providers.dart:13` (`LocalFeedRepository(seed: true)`). `feed_posts` tablosu da yok. | V1 marka vaadi (sosyal feed + ağ etkisi) karşılanmıyor. Mağaza sonrası kullanıcı beklediği davranışı görmez. | (a) `feed_posts` tablosu + RLS + index migration, (b) `SupabaseFeedRepository`, (c) provider seçimini Supabase enabled+user gibi diğer modüllerle hizalama. | `lib/features/feed/**`, yeni `supabase/migrations/2026..._feed.sql` |
| P0 | Sosyal | Sosyal Gruplar yalnız LocalSocialGroupRepository ile çalışıyor. join/leave/message kalıcı değil. | `lib/features/social_groups/providers/social_group_providers.dart:15` | V1 grup deneyimi kullanıcılar arası akmıyor. | groups + members + messages tabloları + repo + provider güncellemesi. | `lib/features/social_groups/**` |
| P0 | İlan | JobsScreen tamamen hardcoded mock listesi; worker_repository ile bağlanmamış. | `lib/features/jobs/screens/jobs_screen.dart:21` (`_bakeriesHiring` / `_bakersLooking` const). | Bottom nav 4. tab production değil. | Aktif `job_seek_posts` listesi + (yeni) `job_offer_posts` tablo; rol bazlı filtreler. | `lib/features/jobs/**` |
| P0 | Mağaza | Marketplace bottom nav 3. tab tamamen statik mock. | `lib/features/marketplace/screens/marketplace_screen.dart:38` (`_items` const). | Reklam edilen "ekipman/hammadde marketi" yok. | (a) marketplace_listings tablosu + repo, (b) detail screen, ya da (c) V1 için tab kaldır. | `lib/features/marketplace/**` |
| P0 | KVKK | Hesap silme akışı yok. | Profile ekranı sadece `signOut`. Hiçbir `deleteUser`/RPC çağrısı yok. Grep `delete account`: 0. | Google Play Data Safety + KVKK denetimi başarısız. | (a) Profile ekranına "Hesabımı sil" düğmesi + confirm dialog, (b) Supabase `admin.deleteUser` (server function) veya `auth.signOut + RPC delete_my_data`, (c) cascade testi. | `lib/features/profile/screens/profile_screen.dart`, yeni RPC migration |
| P0 | Android | Release build debug imzasıyla derlenecek (debug keys). | `android/app/build.gradle.kts:33` `TODO: Add your own signing config for the release build`. `signingConfigs.getByName("debug")`. | Play Store yüklemesi reddedilir veya debug key ile imzalanır. | keystore üret, `key.properties` ekle, `signingConfigs.release` tanımla, `release.signingConfig` ata. | `android/app/build.gradle.kts`, `android/key.properties` (yeni) |
| P0 | Legal | Terms/Privacy ekranlarında "V1 taslaktır ve hukuki final kontrol gerektirir." banner'ı production'a açık. | `lib/features/legal/screens/privacy_screen.dart:38` ve `terms_screen.dart` `_DraftBanner`. | Mağaza incelemecisi taslak metni hukuki kabul olarak görmez. | Hukuki onay sonrası metinleri sabitle, draft banner'ları kaldır, version damga koy. | `lib/features/legal/screens/*.dart` |
| P1 | UX | Profil ekranı "12 Paylaşım / 186 Bağlantı / 4 Yıl" hardcoded. | `lib/features/profile/screens/profile_screen.dart:251` (`_Stats` widget). | Kullanıcı yalan sayı görür; mağaza ihbarına açık. | (a) Sayıları gerçek query'lerle hesapla, (b) ya da `_Stats`'ı tamamen gizle ve "İstatistikler yakında" göster. | `lib/features/profile/screens/profile_screen.dart` |
| P1 | UX | Feed Story strip hardcoded 9 isim. | `lib/features/feed/screens/feed_screen.dart:28` (`_stories` const). | Demo kullanıcı isimleri canlıda kalıyor. | (a) Story strip'i şu an feature yoksa gizle, (b) ya da gerçek "trending users" query'siyle besle. | `lib/features/feed/screens/feed_screen.dart` |
| P1 | Veri Bütünlüğü | `production_date` / `waste_date` `_date(createdAt.toUtc())` ile yazılıyor. | `lib/features/bakery_panel/repositories/supabase_bakery_repository.dart:74` `_date(DateTime d) → dd = d.toUtc()`. | TR'nin 02:00'sında üretim eklenince UTC günü kayar; günsonu özetinde yanlış güne düşer. | TZ aware date: `_date(createdAt.toLocal())` veya `Asia/Istanbul`'a normalize. | `supabase_bakery_repository.dart`, `supabase_dealer_repository.dart` (delivery_date, valid_from) |
| P1 | Legal | Kabul tarihi/versiyonu backend'e yazılmıyor. | `create_profile_screen.dart` `_legalAccepted` yalnızca client state. Submit sonrası backend'e gönderilmiyor. | KVKK denetiminde "kullanıcı şu tarih şu versiyonu kabul etti" log yok. | profile tablosuna `legal_accepted_at`, `legal_version` ya da yeni `legal_acceptances` tablosu. signUp metadata'sına ekle. | `lib/features/profile/screens/create_profile_screen.dart`, migration |
| P1 | İlan | JobsScreen mevcut tablolardaki gerçek ilanları göstermiyor (ne Bireysel'in aktif `job_seek_posts`'unu ne de Ticari'nin "iş veriyorum"unu). | `JobsScreen` hiç repository çağırmıyor. | Bireysel kullanıcı kendi ilanını ana akışta göremez. | İlanlar feature'ı için repo + ortak/akış provider'ı. | `lib/features/jobs/**`, repo provider |
| P1 | Mağaza | Adaptive launcher icon (Android) yok. mipmap-anydpi-v26 dizini eksik. | `android/app/src/main/res` altında sadece klasik mipmap PNG'leri var. | Modern Android launcher'larda mask/round sorunu, brand görseli bozulur. | `flutter_launcher_icons` paketi veya manuel `ic_launcher_foreground.png` + `ic_launcher_background.xml` + `mipmap-anydpi-v26/ic_launcher.xml`. | `android/app/src/main/res/**` |
| P1 | Build | Pubspec name `firin_defter`, version `0.1.0+1`. | `pubspec.yaml:1,5`. | Store için version artırılmalı, name iç identifier kalır ama description ürün ismiyle uyumsuz. | versiyonu `1.0.0+1` veya `0.9.0+1` yap, description'ı "FırınNet — fırıncılar için sektör ağı + işletme defteri" gibi güncelle. | `pubspec.yaml` |
| P1 | Backend doğrulama | Migration dosyalarının canlı remote'a uygulandığı bu session'da doğrulanamadı (MCP yok). | Migration başlıkları "applied via MCP on 2026-05-12" yorumu içeriyor. | Eğer uygulanmadıysa `dealer_transactions` vb. tablolar olmaz, runtime error. | Supabase Studio'dan `supabase_migrations.schema_migrations` tablosunu kontrol et veya `supabase db diff` çalıştır. | (ops) |
| P2 | UX | "Mesajlar — Yakında" 3 rolde gösteriliyor (Ticari, Bireysel, Toptancı). | `lib/features/dashboard/services/role_panel_cards.dart` `cardMessages` `comingSoon: true` her rolde. | Store screenshot'larında olgun görünmez. | Yapılana kadar kartı tamamen gizle veya tek bir yerde "İletişim — yakında" rozetiyle göster. | `role_panel_cards.dart` |
| P2 | UX | HeaderActionButton bazı yerlerde `onTap: () {}` noop. | `bakery_panel_screen.dart:46` (`HeaderActionButton calendar`, noop), `feed_screen.dart:103` (search noop), `marketplace_screen.dart:122` (tune noop), `jobs_screen.dart:114` (add noop). | Kullanıcı butona dokunup hiç tepki almıyor; broken hissi. | Disabled style veya gerçek feature implement et. | İlgili screen dosyaları |
| P2 | RLS | `dealer_transactions/prices/notes` INSERT WITH CHECK'inde `dealer_id` owner cross-check yok. | Migration `20260513200000_..._dealer_v1_2_extensions.sql` policies. | Defense-in-depth eksik; pratik etki düşük. | INSERT/UPDATE policy WITH CHECK'e `EXISTS (... dealers d WHERE d.id = dealer_id AND d.owner_id = auth.uid())`. | Yeni migration |
| P2 | RLS | `worker_profiles`, `worker_experiences` herkese açık; UI başkasının profilini göstermiyor → atıl exposure. | Migration `20260513210000_...` policies `USING (true)`. | API ile profiller toplu çekilebilir. Tasarım netleştirilmeli. | Eğer ileride göstermek istenmiyorsa policy → owner-only veya `is_public` flag. | Yeni migration |
| P2 | UX | Error states feed/groups/jobs'da ham exception mesajı (`'Akış: $e'`). | `feed_screen.dart:386` error builder. | Kullanıcı için anlamsız stack/teknik metin. | EmptyState + Türkçe error copy. | `feed_screen.dart`, `groups_list_screen.dart` |
| P2 | UX | Rol bazlı route koruması yok. | Bireysel kullanıcı manuel `/panel/bakery` URL'sine gidebilir. | Veri sızıntısı değil (RLS owner filtreliyor) ama UX kapatma eksik. | GoRouter redirect içinde role check ya da `RoleGuard` widget. | `app_router.dart`, yeni `role_guard.dart` |
| P2 | Data Integrity | AddDealerScreen save spam → çift insert riski. | `add_dealer_screen.dart:47` `_save()` loading state set etmiyor. | Yan yana iki kez tap → 2 dealer satırı. | `_submitting` flag + button disable. | `add_dealer_screen.dart` |
| P2 | Build | Pubspec description "local-first defter" eski; ürün artık ağ/sosyal. | `pubspec.yaml:2`. | Store description ile uyumsuzluk; SEO. | Description güncelle. | `pubspec.yaml` |
| P3 | Doc | Recipe extras hesabı trigger ↔ UI tutarsızlığı dokümante değil. | `recipe_calculations.estimated_count` trigger ile yüzde formülü, UI client-side extras dahil. | DB raporlama tüketicisi yanlış değer görür. | Migration yorum + repository docstring + (alternatif) trigger formülünü extras-aware yapmak. | `supabase_recipe_repository.dart`, migration |
| P3 | Repo | `firebase-debug.log` kök dizinde. | `dir C:\dev\firinnet`. | Supabase tabanlı projede atıl, gizliyse log içeriği sızabilir. | Sil veya .gitignore. | repo |
| P3 | Repo | Çok sayıda eski `*_REPORT.md` / `*_AUDIT.md` kök dizinde. | dir output 24+ dosya. | Görsel kirlilik. | `docs/` altına taşı veya `archive/`. | repo |
| P3 | Repo | Boş `Cdevfirinnetqa-screenshotsredesign-pass-1/` dizin. | dir output. | Path quoting hatasından artık. | Sil. | repo |
| P3 | Schema | `bakery_products` tablosu Flutter kodu kullanmıyor (dead V1 schema). | Grep `bakery_products` lib altında: yok. | Disk/maintenance. | Kullanım planı varsa tutulur; yoksa cleanup migration. | (ops) |
| P3 | UX | Pubspec `name: firin_defter` ürün ismiyle uyumsuz. | `pubspec.yaml:1`. | İç identifier; etki yok ama tutarsızlık. | (opsiyonel) Tüm referansları kontrol edip `firinnet` olarak rename. | repo geneli |
| P3 | Mağaza | iOS adaptive icon ve onaylanmış IconSet kontrolü yapılmadı. | iOS Runner/Assets.xcassets kontrol edilmedi. | App Store inceleme. | `flutter_launcher_icons` ile tam set üret. | `ios/Runner/Assets.xcassets/AppIcon.appiconset` |
